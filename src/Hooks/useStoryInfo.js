import React from 'react';

// function useStoryInfo(storyData) {
//   const [storyInfo, setStoryInfo] = useState();

//   // 상태관리 로직
//   return; // 여러 파일에서 공유할 상태값 로직(각 파일에서의 상태값은 독립적으로 존재 / 값이 다름)
//   // 가져와서 사용할 때는 이런식으로
//   // const storyInfo = useStoryInfo(story)
// }
